[pytest]
minversion = 8.0
addopts = -ra -q --strict-markers --cov=nano_banana --cov-report=term-missing --cov-report=html --cov-report=xml
testpaths = tests
pythonpath = src
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
